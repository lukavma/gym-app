# Evidence Notes

This file is intentionally a pointer. The evidence↔architecture mapping lives in [`evidence-to-design.md`](./evidence-to-design.md); the scientific interpretation layer itself lives in [`docs/evidence/`](../evidence/) (registry, boundaries, gaps). Keeping a single mapping document avoids a second, drift-prone copy here.
